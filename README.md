# Blustream
Home Assistant integration for Blustream matrix switchers and presentation switches. Control source routing, volume, mute and power through a media player, with per-output switches, auto-switching, microphone controls and output resolution. Local control over TCP/IP, no cloud. Currently supports the MFP62, with more models to follow.
